/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import Calculo from './src/Screens/calculo';

AppRegistry.registerComponent(appName, () => Calculo);
